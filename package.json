{
  "name": "character-count-estimator",
  "version": "1.0.0",
  "description": "Figma plugin that calculates max characters for a text box",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "dev": "npm run watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "figma-plugin-ds": "^1.0.1"
  }
}
